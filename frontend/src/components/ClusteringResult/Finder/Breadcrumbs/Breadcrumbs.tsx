interface breadclumbsProps {
  folderList: string[];
}
const Breadclumbs: React.FC<breadclumbsProps> = ({
  folderList,
}: breadclumbsProps) => {
  console.log(folderList);
  return <></>;
};

export default Breadclumbs;
